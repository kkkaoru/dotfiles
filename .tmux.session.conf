# tmux session configuration for 3 horizontal panes

# Create new window with first pane
new-window -n main

# Split horizontally to create second pane
split-window -v

# Split horizontally again to create third pane
split-window -v

# Select even-vertical layout for equal height panes
select-layout even-vertical

# Select the first pane
select-pane -t 0