# pi agmsg integration

Use pi's globally installed `/agmsg` extension command and the `agmsg` tool.
Never read or modify agmsg database, team, or configuration files directly;
always use the extension, which delegates to agmsg's supported scripts.
